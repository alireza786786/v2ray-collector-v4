# راه‌اندازی ریپوی جدید — V2Ray Collector v4
# نام پیشنهادی ریپو:  v2ray-collector-v4

این راهنما طوری طراحی شده که با **یک بار آپلود**، بقیه‌چیز خودکار شود:
ریپو ← آپلود بسته ← تنظیم ۲ سکرت ← تمام (هر ۶ ساعت خودکار اجرا و جایگزین می‌شود).

---

## مرحله ۱ — ساخت ریپوی جدید (ریپوی قبلی دست نمی‌خورد)

1. مرورگر گوشی → `github.com` → ورود با اکانت خودتان
2. دکمه **+** (بالا سمت راست) → **New repository**
3. در کادر Repository name بنویسید: `v2ray-collector-v4`
4. گزینه **Public** را بگذارید و کادر «Add a README» را **خالی** بگذارید
5. دکمه سبز **Create repository** → حالا یک ریپوی خالی دارید

## مرحله ۲ — آپلود یک‌باره همه فایل‌ها

1. بسته `v4-upload.zip` را دانلود و روی گوشی/کامپیوتر **استخراج** کنید
2. در صفحه ریپوی جدید: **Add file** → **Upload files**
3. این‌ها را انتخاب/کشیدن کنید:
   - پوشه `v2ray_collector` (کل پوشه)
   - پوشه `.github` (کل پوشه)
   - فایل‌ها: `v2ray_collector_v3.py` · `config.example.yaml` · `requirements.txt` · `pyproject.toml` · `.gitignore` · `README.md`
4. دکمه **Commit changes** (پیام دلخواه) → تایید

✅ از همین لحظه GitHub Actions فایل‌ها را دید و دفعه بعدی که اجرا شود، همه‌چیز برپا می‌شود.

## مرحله ۳ — تنظیم سکرت‌ها (فقط ۲ تا)

1. در ریپوی جدید: **Settings** → (سمت چپ) **Secrets and variables** → **Actions**
2. **New repository secret** → نام `BOT_TOKEN` → مقدار = توکن ربات (از @BotFather) — همان توکن قبلی کافی است
3. دوباره New repository secret → نام `CHAT_ID` → مقدار = آیدی کانال (ببینید مرحله ۴)

## مرحله ۴ — ساخت کانال تلگرام جدید (برای مقایسه با کانال قبلی)

1. در تلگرام: New Channel → نام دلخواه مثل `V2Ray V4 Test`
2. ربات قبلی را به‌عنوان **ادمین** کانال اضافه کنید (حق ارسال پیام)
3. اگر کانال عمومی است: `CHAT_ID = @نام‌کانال`
   اگر خصوصی است: آیدی عددی کانال را بگیرید (مثلاً از ربات @userinfobot)
4. همان مقدار را در سکرت `CHAT_ID` ریپوی جدید بگذارید

## مرحله ۵ — اجرای اول (تست)

1. تب **Actions** → «V2Ray Collector» → دکمه **Run workflow** → تأیید
2. چند دقیقه صبر کنید؛ در لاگ مرحله ۶ باید «✅ پارت N ارسال شد» یا «فقط فایل‌ها ساخته شدند» ببینید
3. بعد از اتمام، فایل‌های `subscription_part1..N.txt` و `history.db` خودکار در ریپو کامیت می‌شوند
4. لینک خام هر فایل (`raw.githubusercontent.com/alireza786786/v2ray-collector-v4/main/subscription_part1.txt`) همان لینک اشتراک است

## از این به بعد — خودکار

| کار | چه کسی |
|---|---|
| هر ۶ ساعت اجرا | workflow (زمان‌بندی در run.yml) |
| ساخت و **جایگزینی** فایل‌های اشتراک | مرحله Commit and push |
| ارسال به کانال جدید | ربات با سکرت‌ها |

## مقایسه (چرا این ریپو جدا است)

| | ریپوی قبلی | ریپوی جدید |
|---|---|---|
| کد | v3 (تک‌فایلی) | v4 (ماژولار) |
| کانال تلگرام | کانال قبلی | کانال جدید |
| history.db | جدا | جدا (مستقل شروع می‌شود) |

هر دو هم‌زمان کار می‌کنند و می‌توانید کیفیت خروجی‌ها را کنار هم مقایسه کنید.

## عیب‌یابی سریع

- لاگ: «BOT_TOKEN یا CHAT_ID تنظیم نشده» → سکرت‌ها را چک کنید (مرحله ۳)
- «دانلود Xray ناموفق» → مشکل موقت شبکه؛ مرحله تست واقعی رد می‌شود و بقیه کار می‌کند
- خطای دیگری دیدید → خروجی Run workflow را در چت بفرستید تا بررسی کنم